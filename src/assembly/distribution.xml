<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.1.1"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.1.1 http://maven.apache.org/xsd/assembly-2.1.1.xsd">
    <id>dist</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>true</includeBaseDirectory>
    <baseDirectory>brace-${project.version}</baseDirectory>

    <fileSets>
        <!-- Launcher script -->
        <fileSet>
            <directory>${project.basedir}/bin</directory>
            <outputDirectory>bin</outputDirectory>
            <fileMode>0755</fileMode>
            <includes>
                <include>brace</include>
            </includes>
        </fileSet>

        <!-- Migration guides — one file per release transition -->
        <fileSet>
            <directory>${project.basedir}/docs/migrations</directory>
            <outputDirectory>migrations</outputDirectory>
            <includes>
                <include>brace-*.md</include>
            </includes>
        </fileSet>

        <!-- Agent-facing framework reference — also packaged inside the brace jar at
             /brace/BRACE-AGENTS.md (which `brace agents-md` extracts); shipped here too
             so the doc is browsable without opening the jar. -->
        <fileSet>
            <directory>${project.basedir}</directory>
            <outputDirectory></outputDirectory>
            <includes>
                <include>BRACE-AGENTS.md</include>
            </includes>
        </fileSet>
    </fileSets>

    <files>
        <!-- Ops reference companion — packaged inside the brace jar at
             /brace/agent-ops-guide.md and written into projects as BRACE-OPS.md;
             shipped in the zip under the project-facing name. -->
        <file>
            <source>${project.basedir}/docs/agent-ops-guide.md</source>
            <destName>BRACE-OPS.md</destName>
        </file>
    </files>

    <dependencySets>
        <!-- All runtime + compile scope deps (excludes test scope like h2) -->
        <dependencySet>
            <outputDirectory>lib</outputDirectory>
            <useProjectArtifact>true</useProjectArtifact>
            <scope>runtime</scope>
            <unpack>false</unpack>
        </dependencySet>
    </dependencySets>
</assembly>
